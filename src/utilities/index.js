module.exports = {
  arrays: require('./arrays'),
  language: require('./language'),
  scope: require('./scope'),
  split: require('./split'),
  strings: require('./strings'),
  version: require('./version')
}
